{
  "name": "toolshop-agents-ci",
  "version": "0.0.0",
  "private": true,
  "description": "AI-generated Playwright tests, shaped through review and proven against known defects.",
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "playwright test --project=ui --project=api-clean",
    "test:ui": "playwright test --project=ui",
    "test:api": "playwright test --project=api-clean",
    "bug-hunt": "mkdir -p reports && playwright test --project=api-buggy --reporter=json > reports/buggy.json; node scripts/assert-bugs-caught.ts reports/buggy.json",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "guardrails": "node scripts/assert-guardrails-bite.ts"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@types/node": "26.2.0",
    "eslint": "10.8.1",
    "eslint-plugin-playwright": "2.11.0",
    "typescript": "6.0.3",
    "typescript-eslint": "8.66.0"
  }
}
